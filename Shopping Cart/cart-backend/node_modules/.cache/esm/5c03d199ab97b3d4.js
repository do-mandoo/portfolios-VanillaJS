let mongoose;_ab9‍.x([["default",()=>_ab9‍.o]]);_ab9‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const { Schema } = mongoose;

const ProductSchema = new Schema({
  name: String,
  price: Number,
  imageUrl: String,
  publishedData: {
    type: Date,
    default: Date.now // 현재 날짜를 기본값으로 지정
  }
});

const Product = mongoose.model('Product', ProductSchema);
_ab9‍.d(Product);
