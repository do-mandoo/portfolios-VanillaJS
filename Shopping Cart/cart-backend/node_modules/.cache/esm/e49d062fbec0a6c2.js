let mongoose;_bec‍.x([["default",()=>_bec‍.o]]);_bec‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const { Schema } = mongoose;

const CartSchema = new Schema({
  cartId: String,
  numberOfCart: Number,
  name: String,
  price: Number,
  imageUrl: String
});

const Cart = mongoose.model('Cart', CartSchema);
_bec‍.d(Cart);
